databaseChangeLog:
  - changeSet:
      id: add-must-change-password
      author: saascore
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: must_change_password
                  type: boolean
                  defaultValueBoolean: false
