# Consolidated: image build & push moved to build-push.yml.
# This workflow is kept only as a redirect / reference.
# To push images to GHCR on main, see build-push.yml.
name: Deploy (deprecated — use build-push.yml)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow has been consolidated into build-push.yml."
          echo "Trigger build-push.yml instead for Docker image builds."
          echo "See: .github/workflows/build-push.yml"
